# Agent Deployment Architecture

## Repository Structure

```
ai-maf-poc-agent/
│
├── 🏗️  infrastructure/                    Infrastructure as Code (Connections)
│   │
│   ├── nonprod.bicepparam                 ← Single configuration file
│   │                                        • Connection name: weathertool
│   │                                        • Target URL: wttr.in
│   │                                        • OpenAPI spec path
│   │                                        • Tags and metadata
│   │
│   └── modules/
│       └── foundry_connection/
│           ├── connection.bicep           ← Bicep template (HOW to deploy)
│           └── api.json                   ← OpenAPI 3.1 specification
│                                            (Weather API schema)
│
├── 🤖  agents/                            Agent Definitions (Business Logic)
│   │
│   └── weather-agent.yaml                 ← Agent definition
│                                            • Model: gpt-4o-mini
│                                            • Instructions/prompts
│                                            • Tool references (connection_id)
│                                            • No infrastructure details!
│
└── ⚙️   scripts/                           Deployment Automation
    │
    ├── deploy_infrastructure.py           ← Deploy connections via Bicep
    ├── deploy_agent.py                    ← Deploy agents via SDK
    ├── tool_factory.py                    ← Convert YAML → SDK objects
    └── local-deploy.sh                    ← Interactive deployment menu
```

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  1️⃣  INFRASTRUCTURE LAYER (What tools are available)                    │
│                                                                         │
│   ┌──────────────────────┐   ┌──────────────────┐   ┌───────────────┐ │
│   │  nonprod.bicepparam  │──▶│ connection.bicep │◀──│   api.json    │ │
│   │                      │   │                  │   │               │ │
│   │ • Connection config  │   │ Bicep template   │   │ OpenAPI spec  │ │
│   │ • Project name       │   │ Resource def     │   │ API schema    │ │
│   │ • Tags               │   │ Properties       │   │               │ │
│   └──────────────────────┘   └──────────────────┘   └───────────────┘ │
│                                       │                                 │
│                                       │ Deploy via Bicep                │
│                                       ▼                                 │
└───────────────────────────────────────────────────────────────────────┘
                                        │
                                        │
┌───────────────────────────────────────────────────────────────────────┐
│                                       ▼                                │
│  3️⃣  AZURE AI FOUNDRY (Deployed Resources)                             │
│                                                                        │
│   ┌────────────────────────────────────────────────┐                  │
│   │  🔗 Connection: weathertool                    │                  │
│   │                                                 │                  │
│   │  • Target: https://wttr.in                     │                  │
│   │  • Auth: CustomKeys                            │                  │
│   │  • OpenAPI spec embedded                       │                  │
│   │  • Status: Active ✅                            │                  │
│   └────────────────────────────────────────────────┘                  │
│                           ▲                                            │
│                           │ Must exist first!                          │
│                           │                                            │
│   ┌────────────────────────────────────────────────┐                  │
│   │  🤖 Agent: weather-agent                       │                  │
│   │                                                 │                  │
│   │  • Model: gpt-4o-mini                          │                  │
│   │  • Instructions: weather assistant prompts     │                  │
│   │  • Tools: [weathertool] ◀─────────────────┐   │                  │
│   │  • Status: Deployed ✅                       │   │                  │
│   └────────────────────────────────────────────────┘                  │
│                           ▲                         │                  │
└───────────────────────────────────────────────────────────────────────┘
                            │                         │
                            │ Deploy via SDK          │
                            │                         │ References by ID
┌───────────────────────────────────────────────────────────────────────┐
│                           │                         │                  │
│  2️⃣  AGENT LAYER (What agents do)                  │                  │
│                                                     │                  │
│   ┌─────────────────────────────────────────────────────────┐         │
│   │  weather-agent.yaml                                     │         │
│   │                                                          │         │
│   │  model: gpt-4o-mini                                     │         │
│   │  instructions: "You are a weather assistant..."         │         │
│   │  tools:                                                 │         │
│   │    - type: openapi                                      │         │
│   │      id: weathertool                                    │         │
│   │      connection_id: weathertool  ◀──────────────────────┘         │
│   │                                                          │         │
│   └─────────────────────────────────────────────────────────┘         │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘

Key Points:
  ═══▶  Deploy First (Infrastructure)
  ───▶  Deploy Second (Agent)
  ◀───  References/Uses
```

## Deployment Sequence (Critical Order)

```
╔══════════════════════════════════════════════════════════════════════════╗
║                  PHASE 1: Deploy Connection (MUST BE FIRST!)             ║
╚══════════════════════════════════════════════════════════════════════════╝

👨‍💻 Developer                                    ☁️  Azure Resource Manager
    │                                                │
    │ 1. Configure connection                        │
    │    • name: weathertool                         │
    │    • target: wttr.in                           │
    │    • OpenAPI spec: api.json                    │
    │                                                │
    │ 2. Run deployment command:                     │
    │    python scripts/deploy_infrastructure.py \   │
    │      foundry_connection \                      │
    │      --bicepparam nonprod.bicepparam           │
    │                                                │
    ├────────────── Bicep Deployment ──────────────▶│
    │                                                │
    │                                                │ 3. Create connection
    │                                                │    resource
    │                                                │
    │                                                │ 4. Register in
    │                                                ├──▶ 🎯 Azure AI Foundry
    │                                                │
    │◀──────── ✅ Connection Ready ──────────────────│
    │                                                │
    │   Connection "weathertool" is now available!   │
    │                                                │

╔══════════════════════════════════════════════════════════════════════════╗
║            PHASE 2: Deploy Agent (DEPENDS ON CONNECTION)                 ║
╚══════════════════════════════════════════════════════════════════════════╝

👨‍💻 Developer                                    🎯 Azure AI Foundry
    │                                                │
    │ 5. Define agent with tool reference:           │
    │                                                │
    │    # weather-agent.yaml                        │
    │    tools:                                      │
    │      - type: openapi                           │
    │        connection_id: weathertool  ◀───────────┼─── Must exist!
    │                                                │
    │ 6. Run deployment command:                     │
    │    python -m scripts.deploy_agent \            │
    │      <endpoint> \                              │
    │      agents/weather-agent.yaml                 │
    │                                                │
    ├─────────── SDK Deployment (API call) ────────▶│
    │                                                │
    │                                                │ 7. Validate connection
    │                                                │    "weathertool" exists
    │                                                │
    │                                                │ 8. Attach weathertool
    │                                                │    to agent
    │                                                │
    │                                                │ 9. Create agent with
    │                                                │    tools attached
    │                                                │
    │◀────────── ✅ Agent Ready ─────────────────────│
    │                                                │
    │   Agent "weather-agent" is now deployed!       │
    │   Can now answer weather queries!              │


⚠️  CRITICAL: If you try to deploy agent BEFORE connection exists:
    ❌ Error: Connection "weathertool" not found
    ❌ Agent deployment will FAIL
```

## Separation of Concerns

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🏗️  INFRASTRUCTURE LAYER (Platform Team)                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

    Responsibilities:
    ┌────────────────────────────────────────────────────────────┐
    │ • Define what tools/APIs are available                     │
    │ • Configure authentication & security                      │
    │ • Manage OpenAPI specifications                            │
    │ • Deploy infrastructure as code (Bicep)                    │
    │ • Handle Azure resource lifecycle                          │
    └────────────────────────────────────────────────────────────┘

    Files:
      infrastructure/
      ├── nonprod.bicepparam     → Configuration
      └── modules/
          └── foundry_connection/
              ├── connection.bicep → Template
              └── api.json        → OpenAPI spec

    Deployment:
      $ python scripts/deploy_infrastructure.py foundry_connection \
          --bicepparam infrastructure/nonprod.bicepparam

                            │
                            │ Provides connections
                            ▼
    ┌─────────────────────────────────────────────────────────┐
    │   🔗 Azure AI Foundry Connections                        │
    │                                                          │
    │   • weathertool                                          │
    │   • database-api                                         │
    │   • payment-gateway                                      │
    └─────────────────────────────────────────────────────────┘
                            │
                            │ Referenced by agents
                            ▼

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🤖 AGENT LAYER (Application Team)                                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

    Responsibilities:
    ┌────────────────────────────────────────────────────────────┐
    │ • Define agent behavior & personality                      │
    │ • Write instructions/prompts                               │
    │ • Select AI models                                         │
    │ • Reference tools by connection_id only                    │
    │ • NO knowledge of infrastructure details!                  │
    └────────────────────────────────────────────────────────────┘

    Files:
      agents/
      └── weather-agent.yaml  → Agent definition
                                 • Model config
                                 • Instructions
                                 • Tool references (ID only!)

    Deployment:
      $ python -m scripts.deploy_agent \
          https://.../api/projects/my-project \
          agents/weather-agent.yaml


────────────────────────────────────────────────────────────────────────

WHY SEPARATE?

✅  Reusability:  One connection → Many agents can use it
✅  Security:     Credentials managed centrally by platform team
✅  Maintenance:  Update connection without touching 10+ agents
✅  Testing:      Swap dev/prod connections easily
✅  Ownership:    Clear boundaries between teams
```

## Connection → Tool → Agent Flow

```
╔══════════════════════════════════════════════════════════════════════════╗
║  STEP 1: Create Connection (Infrastructure)                             ║
╚══════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────┐
    │  nonprod.bicepparam             │
    │                                 │
    │  connectionName: weathertool    │
    │  targetUrl: wttr.in             │
    │  openApiSpec: api.json          │
    │  authType: CustomKeys           │
    └─────────────────────────────────┘
                  │
                  │  1. Load config
                  ▼
    ┌─────────────────────────────────┐
    │  connection.bicep               │
    │                                 │
    │  Bicep template defines:        │
    │  • Resource type                │
    │  • Properties                   │
    │  • Metadata                     │
    └─────────────────────────────────┘
                  │
                  │  2. Deploy to Azure
                  ▼
    ┌─────────────────────────────────┐
    │  ☁️  Azure AI Foundry            │
    │                                 │
    │  Connection: weathertool        │
    │  Status: Active ✅              │
    └─────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║  STEP 2: Reference in Agent (Application)                               ║
╚══════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────┐
    │  weather-agent.yaml             │
    │                                 │
    │  name: weather-agent            │
    │  model: gpt-4o-mini             │
    │  tools:                         │
    │    - type: openapi              │
    │      id: weathertool            │
    │      connection_id: weathertool │─┐
    └─────────────────────────────────┘ │
                  │                     │  Just a reference!
                  │  3. Parse YAML      │  No URL, auth, or
                  ▼                     │  OpenAPI spec needed
    ┌─────────────────────────────────┐ │
    │  tool_factory.py                │ │
    │                                 │ │
    │  Converts YAML → SDK objects:   │ │
    │  • OpenApiAgentTool             │◀┘
    │  • References connection by ID  │
    └─────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║  STEP 3: Deploy Agent with Tool (SDK Call)                              ║
╚══════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────┐
    │  deploy_agent.py                │
    │                                 │
    │  4. Send agent definition       │
    │     to Azure AI Foundry         │
    └─────────────────────────────────┘
                  │
                  │  5. API call to Foundry
                  ▼
    ┌─────────────────────────────────┐
    │  🔗 Azure AI Foundry Validation │
    │                                 │
    │  6. Check: Does connection      │
    │     "weathertool" exist?        │─────┐
    └─────────────────────────────────┘     │
                  │                         │
                  │  ✅ Yes!                 │
                  ▼                         │
    ┌─────────────────────────────────┐     │
    │  7. Attach weathertool to agent │◀────┘
    └─────────────────────────────────┘
                  │
                  │  8. Create agent
                  ▼
    ┌─────────────────────────────────┐
    │  ✅ Agent Created Successfully   │
    │                                 │
    │  Agent: weather-agent           │
    │  Tools: [weathertool]           │
    │  Status: Deployed               │
    └─────────────────────────────────┘


────────────────────────────────────────────────────────────────────────

💡 Key Insight:

  Connection (Step 1) ───▶ MUST exist BEFORE ───▶ Agent (Step 3)
                                              
  If connection doesn't exist:
    ❌ Error: Connection "weathertool" not found
    ❌ Agent deployment FAILS
```

## File Dependencies and Deployment Path

```
📁 Configuration Files
│
├── nonprod.bicepparam ──────┐
│   (Connection config)       │
│                             │
├── weather-agent.yaml ───────┼───────┐
│   (Agent definition)        │       │
│                             │       │
└── api.json ─────────────────┼───┐   │
    (OpenAPI spec)            │   │   │
                              │   │   │
                              │   │   │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                              │   │   │
📄 Infrastructure Templates   │   │   │
│                             ▼   ▼   │
└── connection.bicep ────────●───●   │
    (Bicep template)                  │
                              │       │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                              │       │
⚙️  Deployment Scripts        │       ▼
│                             │   
├── deploy_infrastructure.py ●──────────────┐
│   (Deploys connections)                   │
│                                           │
├── deploy_agent.py ──────────────────●─────┼───┐
│   (Deploys agents)                        │   │
│                                           │   │
└── tool_factory.py ──────────────────●◀────┘   │
    (YAML→SDK converter)                        │
                                                │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                                                │
🔄 CI/CD Pipelines                              │
│                                               │
├── infrastructure-pipeline.yml ───●───────────┘
│   (Triggers deploy_infrastructure.py)         
│                                               
├── agent-pipeline.yml ────────────────●────────┘
│   (Triggers deploy_agent.py)
│
└── azure-pipelines.yml ───────────────●────────┐
    (Unified: both stages)                      │
                                                │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                                                │
☁️  Azure Resources (Created)                   │
│                                               ▼
├── Connection: weathertool ◀───────────────────●
│   (Deployed by Bicep)                         
│                             ┌─────────────────┐
│                             │ Must exist      │
│                             │ BEFORE agent!   │
│                             └────────┬────────┘
│                                      │
└── Agent: weather-agent ◀─────────────┼─────────●
    (Deployed by SDK)                  │
                                       │
    Tools: [weathertool] ◀─────────────┘
    (References connection)


Legend:
  ───●──▶  Reads/uses file or calls script
  ◀───────  References or depends on
  ━━━━━━━  Layer boundary
```

## Component Interaction Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│  👨‍💻 Developer Experience                                             │
└──────────────────────────────────────────────────────────────────────┘

    nonprod.bicepparam              weather-agent.yaml
    ┌────────────────────┐          ┌─────────────────────┐
    │ • connectionName   │          │ • name              │
    │ • targetUrl        │          │ • model             │
    │ • authType         │          │ • instructions      │
    │ • openApiSpec      │          │ • tools[]           │
    └────────────────────┘          └─────────────────────┘
              │                               │
              │ Read by                       │ Read by
              ▼                               ▼


┌──────────────────────────────────────────────────────────────────────┐
│  ⚙️  Deployment Layer                                                 │
└──────────────────────────────────────────────────────────────────────┘

    deploy_infrastructure.py        tool_factory.py ◀─┐
    ┌────────────────────┐          ┌──────────────┐  │
    │ 1. Load bicepparam │          │ Convert YAML │  │
    │ 2. Call az CLI     │          │ to SDK objs  │  │
    │ 3. Deploy Bicep    │          └──────────────┘  │
    └────────────────────┘                  │         │
              │                             │         │
              │ Calls                       │ Used by │
              ▼                             ▼         │
                                                      │
                                  deploy_agent.py ────┘
                                  ┌────────────────────┐
                                  │ 1. Load agent YAML │
                                  │ 2. Build tools     │
                                  │ 3. Call SDK API    │
                                  └────────────────────┘
              │                             │
              │                             │
              ▼                             ▼


┌──────────────────────────────────────────────────────────────────────┐
│  ☁️  Azure Services                                                   │
└──────────────────────────────────────────────────────────────────────┘

    Azure Resource Manager          Azure AI Foundry SDK
    ┌────────────────────┐          ┌──────────────────┐
    │ Bicep deployment   │          │ Agent API        │
    │ ARM template       │          │ Project API      │
    │ Resource creation  │          │ Authentication   │
    └────────────────────┘          └──────────────────┘
              │                             │
              │ Creates                     │ Creates
              ▼                             ▼


┌──────────────────────────────────────────────────────────────────────┐
│  🎯 Created Resources                                                 │
└──────────────────────────────────────────────────────────────────────┘

    Connection: weathertool         Agent: weather-agent
    ┌─────────────────────────┐     ┌──────────────────────────┐
    │ • Target: wttr.in       │     │ • Model: gpt-4o-mini      │
    │ • Auth: CustomKeys      │     │ • Instructions: defined   │
    │ • OpenAPI spec embedded │     │ • Tools: [weathertool] ◀──┼──┐
    │ • Status: Active        │     │ • Status: Deployed        │  │
    └─────────────────────────┘     └──────────────────────────┘  │
              ▲                                                     │
              │                                                     │
              └─────────────────────────────────────────────────────┘
                           References by connection_id


Key Relationships:

  • nonprod.bicepparam   ──▶  deploy_infrastructure.py  ──▶  Connection
  • weather-agent.yaml   ──▶  deploy_agent.py           ──▶  Agent
  • weather-agent.yaml   ──▶  tool_factory.py           ──▶  SDK objects
  • Connection           ──▶  Agent                      (via reference)
```

## Pipeline Trigger Logic

```
                          📝 Code Change
                                │
                                │
        ┌───────────────────────┼────────────────────────┐
        │                       │                        │
        │                       │                        │
        ▼                       ▼                        ▼
   infrastructure/**       agents/**               Both paths
     changed              changed                  changed
        │                       │                        │
        │                       │                        │
        ▼                       ▼                        ▼
        
┌─────────────────┐  ┌──────────────────┐  ┌────────────────────┐
│ Infrastructure  │  │  Agent Pipeline  │  │ Unified Pipeline   │
│ Pipeline        │  │                  │  │                    │
└─────────────────┘  └──────────────────┘  └────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Pipeline 1: infrastructure-pipeline.yml                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Trigger: infrastructure/**                                         │
│                                                                     │
│  Steps:                                                             │
│    1. Checkout code                                                 │
│    2. Setup Python                                                  │
│    3. Install dependencies                                          │
│    4. Azure CLI login (service connection)                          │
│    5. Deploy connection:                                            │
│       python scripts/deploy_infrastructure.py foundry_connection \  │
│         --bicepparam infrastructure/nonprod.bicepparam              │
│                                                                     │
│  Result: weathertool connection deployed ✅                         │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Pipeline 2: agent-pipeline.yml                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Trigger: agents/** or scripts/**                                   │
│                                                                     │
│  Prerequisites: ⚠️  Connection MUST exist already!                  │
│                                                                     │
│  Steps:                                                             │
│    1. Checkout code                                                 │
│    2. Setup Python                                                  │
│    3. Install dependencies                                          │
│    4. Azure CLI login (service connection)                          │
│    5. Deploy agent:                                                 │
│       .venv/bin/python -m scripts.deploy_agent \                    │
│         "$FOUNDRY_ENDPOINT" \                                       │
│         agents/weather-agent.yaml                                   │
│                                                                     │
│  Result: weather-agent deployed with weathertool ✅                 │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  Pipeline 3: azure-pipelines.yml (Unified)                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Trigger: Manual or both infrastructure/ and agents/ changed        │
│                                                                     │
│  ╔═══════════════════════════════════════════════════════════════╗ │
│  ║  Stage 1: DeployInfrastructure                                ║ │
│  ╚═══════════════════════════════════════════════════════════════╝ │
│    1. Checkout code                                                 │
│    2. Setup Python                                                  │
│    3. Install dependencies                                          │
│    4. Azure CLI login                                               │
│    5. Deploy connection via Bicep                                   │
│    6. Wait for completion                                           │
│                                                                     │
│                         │                                           │
│                         │ Stage dependency                          │
│                         ▼                                           │
│                                                                     │
│  ╔═══════════════════════════════════════════════════════════════╗ │
│  ║  Stage 2: DeployAgent (depends on Stage 1)                    ║ │
│  ╚═══════════════════════════════════════════════════════════════╝ │
│    1. Checkout code                                                 │
│    2. Setup Python                                                  │
│    3. Install dependencies                                          │
│    4. Azure CLI login                                               │
│    5. Deploy agent via SDK                                          │
│                                                                     │
│  Result: Complete deployment ✅                                     │
│          Connection + Agent both deployed in order                  │
└─────────────────────────────────────────────────────────────────────┘


⚠️  IMPORTANT:
    • Agent pipeline assumes connection exists (manual coordination needed)
    • Unified pipeline ensures correct order (connection → agent)
    • For first-time setup, use unified pipeline
```

## Why This Architecture?

```
┌─────────────────────────────────────────────────────────────────────┐
│  ❓ THE PROBLEM                                                      │
└─────────────────────────────────────────────────────────────────────┘

    Agents need external tools to perform real actions:
    
    ┌────────────────┐
    │  Weather Agent │  Needs to call Weather API
    └────────────────┘
    
    ┌────────────────┐
    │  Sales Agent   │  Needs to call Payment API
    └────────────────┘
    
    ┌────────────────┐
    │  Support Agent │  Needs to call Database API
    └────────────────┘
    
    BUT: Each API requires:
      • URL endpoints
      • Authentication credentials
      • OpenAPI specifications
      • Security configuration
      
    ❌ Problem: Agents shouldn't hardcode infrastructure details!


┌─────────────────────────────────────────────────────────────────────┐
│  💡 THE SOLUTION                                                     │
└─────────────────────────────────────────────────────────────────────┘

    Separate Infrastructure from Business Logic
    
    ┌────────────────────────────────┐     ┌─────────────────────────┐
    │  🏗️  INFRASTRUCTURE LAYER       │     │  🤖 APPLICATION LAYER   │
    │                                │     │                         │
    │  Manages CONNECTIONS:          │     │  Manages AGENTS:        │
    │  • API endpoints               │     │  • Behavior             │
    │  • Authentication              │     │  • Instructions         │
    │  • OpenAPI specs               │     │  • Model selection      │
    │  • Credentials                 │────▶│  • Tool references      │
    │                                │     │    (by ID only!)        │
    └────────────────────────────────┘     └─────────────────────────┘
           (Platform Team)                      (App Developers)


┌─────────────────────────────────────────────────────────────────────┐
│  ✅ THE BENEFITS                                                     │
└─────────────────────────────────────────────────────────────────────┘

    1. 🔄 REUSABILITY
       ┌──────────────────┐
       │  weathertool     │─────┬───▶ Weather Agent
       │  (one connection)│     │
       └──────────────────┘     ├───▶ Travel Agent
                                └───▶ Event Planner Agent
       
       One connection → Multiple agents can use it
       
    
    2. 🔒 SECURITY
       ┌────────────────────────────────────────────┐
       │  Credentials stored in ONE place           │
       │  • Platform team controls access           │
       │  • App developers never see credentials    │
       │  • Rotate credentials without code changes │
       └────────────────────────────────────────────┘
       
    
    3. 🛠️ MAINTENANCE
       Update connection:        Impact on agents:
       • Change API endpoint  →  ZERO changes needed
       • Update auth method   →  ZERO changes needed
       • Fix OpenAPI spec     →  ZERO changes needed
       
       Agents just reference connection_id: "weathertool"
       
    
    4. 🧪 TESTING
       ┌─────────────┐         ┌─────────────┐
       │  Dev Agent  │───▶     │  Dev API    │
       └─────────────┘         └─────────────┘
              │
              │ Deploy to prod
              ▼
       ┌─────────────┐         ┌─────────────┐
       │  Prod Agent │───▶     │  Prod API   │
       └─────────────┘         └─────────────┘
       
       Same agent code, different connection config!
```

## Key Design Principles

### 1. **Separation of Concerns**
```
infrastructure/          → What tools are available (HOW to connect)
agents/                  → What agents do with tools (WHAT to do)
```

### 2. **Deployment Order Matters**
```
1. Connection FIRST  → Create the tool capability
2. Agent SECOND      → Use the tool capability
```

### 3. **Loose Coupling**
```yaml
# Agent only knows connection name, not implementation details
tools:
  - type: openapi
    connection_id: weathertool  # ← Just a reference!
```

### 4. **Single Source of Truth**
```
nonprod.bicepparam  → All environment configuration
weather-agent.yaml  → All agent behavior
```

## Real-World Scenario

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🏗️  INFRASTRUCTURE TEAM                                            ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

    Creates and manages connections:
    
    ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
    │  weathertool        │  │  customer-db        │  │  payment-gateway    │
    │                     │  │                     │  │                     │
    │  https://wttr.in    │  │  SQL Database       │  │  Stripe API         │
    │  CustomKeys auth    │  │  Managed Identity   │  │  API Key auth       │
    └─────────────────────┘  └─────────────────────┘  └─────────────────────┘
              │                        │                        │
              │                        │                        │
              └────────────────────────┼────────────────────────┘
                                       │
                        Connections available in catalog
                                       │
                                       ▼

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🤖 APPLICATION TEAM                                                ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

    Builds agents that reference connections:
    
    ┌──────────────────────────┐
    │  trip-planner-agent      │
    │                          │
    │  tools:                  │
    │    - weathertool  ───────┼──┐
    └──────────────────────────┘  │  References weathertool
                                  │
    ┌──────────────────────────┐  │
    │  customer-service-agent  │  │
    │                          │  │
    │  tools:                  │  │
    │    - customer-db  ───────┼──┼──┐
    │    - weathertool  ───────┼──┘  │  References customer-db
    └──────────────────────────┘      │
                                      │
    ┌──────────────────────────┐      │
    │  sales-assistant-agent   │      │
    │                          │      │
    │  tools:                  │      │
    │    - payment-gateway ────┼──────┼──┐
    │    - customer-db  ───────┼──────┘  │  References payment-gateway
    └──────────────────────────┘          │


════════════════════════════════════════════════════════════════════════

RESULT: Flexible, Maintainable System

┌────────────────────────────────────────────────────────────────────┐
│  ✅ One connection, multiple agents can use it                      │
│     (weathertool used by trip-planner AND customer-service)        │
│                                                                    │
│  ✅ Platform team updates connections, app team unaffected         │
│     (Change weathertool URL → No agent code changes needed)        │
│                                                                    │
│  ✅ Security managed centrally                                     │
│     (App developers never see API keys)                            │
│                                                                    │
│  ✅ Easy environment switching                                     │
│     (Same agent, different connection config for dev/prod)         │
└────────────────────────────────────────────────────────────────────┘


Example Workflow:

  Day 1: Infrastructure team deploys weathertool connection
         ↓
  Day 2: App team builds trip-planner-agent using weathertool
         ↓
  Day 5: App team builds customer-service-agent, reuses weathertool
         ↓
  Day 10: Weather API changes endpoint
          Infrastructure team updates weathertool connection
          ✅ Both agents continue working (no code changes!)
```
